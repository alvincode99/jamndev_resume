import StratosHomeShell from "@/components/stratos/home-shell";

/**
 * Renderiza el home base de Stratos para usarlo como punto de partida visual.
 * @returns Estructura principal del inicio con navegacion lateral y hero.
 */
export default function HomePage() {
  return <StratosHomeShell />;
}
